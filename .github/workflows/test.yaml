name: Test
on:
  push:
    branches: [ "feat/add-initial-setup" ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test Version
        run: |
          docker-compose run --rm web odoo -i base -d test_db --stop-after-init
          docker-compose run --rm web odoo -u base -d test_db --test-enable --stop-after-init
